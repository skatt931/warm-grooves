{
  "name": "warm-grooves",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/build-sw.js",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "catalogue": "python3 scripts/prepare_catalogue.py",
    "test:browser": "node scripts/test-browser.js"
  },
  "devDependencies": {
    "@playwright/cli": "^0.1.19",
    "vite": "^8.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
